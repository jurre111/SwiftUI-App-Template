import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome!")
                .font(.title)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}

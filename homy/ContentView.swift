import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Welcome to Homy")
                .font(.title)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
